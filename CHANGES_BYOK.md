# TANGO-CHO v48.0.1 BYOK版 — APIキー保存修正

- APIキーとAI設定の保存先をIndexedDB優先に変更
- 単語帳データでlocalStorage容量が逼迫していても保存しやすく改善
- IndexedDB非対応時はlocalStorageへ自動フォールバック
- 保存結果をAI設定欄の直下に表示
- 「未保存の変更あり」「APIキー保存済み」を明確に表示
- 保存後に読み戻して検証
- Enterキーでも設定を保存可能
- PWAキャッシュを `v48.0.1-byok-storage-fix` に更新

# TANGO-CHO v48.0.0 BYOK版 — 変更内容

## 追加・変更

- 固定の外部AIサービス／キーワード方式を廃止
- 利用者自身のOpenAI APIキーで動作するBYOK設定を追加
- Responses APIによる日本語訳
- 類義語・近義語の取得
- 意味・類義語・例文・ニュアンスメモのAI一括補完
- 例文レベルを「中学生・高校生・大人」から選択可能
- 類義語カード作成時の日本語訳を一括リクエスト化し、API呼び出し回数を削減
- APIキーの表示／非表示、保存、接続テスト、削除を追加
- APIキーを単語帳バックアップJSONから除外
- PWAキャッシュを `v48.0.0-byok` に更新

## 使い方

1. 「追加」タブの「AI設定（BYOK）」を開く
2. OpenAI APIキーを入力する
3. モデルと例文レベルを選ぶ
4. 「接続テスト」を押す
5. 「設定を保存」を押す

## セキュリティ上の注意

この版は静的PWAからOpenAI APIへ直接通信する、個人利用・自己ホスト向けの構成です。APIキーはブラウザの `localStorage` に保存されます。共有端末では保存せず、専用キーと低めの利用上限を設定してください。一般公開サービスとして運用する場合は、APIキーをブラウザに置かず、バックエンド経由へ変更してください。

## 実施した確認

- `script.js` / `service-worker.js` のJavaScript構文確認
- HTMLの重複IDとBYOK操作要素の存在確認
- `manifest.json` と語彙JSONの構文確認
- Responses APIのモックテスト
  - 翻訳
  - 類義語
  - 一括補完
  - 類義語の一括翻訳
  - APIキー・モデル・`store: false` の送信
  - 401エラーの日本語表示

実際のOpenAI APIキーを使った接続試験は行っていません。
